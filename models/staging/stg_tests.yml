version: 2

models:
  - name: stg_order
    columns:
      - name: Orderid
        tests:
          - unique
          - not_null
      - name: Createddate
        tests:
          - not_null